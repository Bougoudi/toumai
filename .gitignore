node_modules/
dist/
*.log

# Environnement
.env
.env.local

# Base de données SQLite locale
prisma/dev.db
prisma/dev.db-journal
*.sqlite
*.sqlite-journal

# Divers
.DS_Store
coverage/

# Captures du test navigateur
.captures/

# Pièces jointes de la messagerie (stockage privé local)
var/touma-attachments/

# Sortie de compilation de la vitrine Next.js. Elle était suivie par accident :
# 156 fichiers régénérés à chaque build, que rien ne lit — ni le Dockerfile
# (qui installe `--workspaces=false` et ne contient pas la vitrine), ni
# l'intégration continue (qui recompile), ni `render.yaml`.
apps/web/.next/
*.tsbuildinfo
